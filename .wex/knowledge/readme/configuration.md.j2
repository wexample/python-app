## Configuration

Configuration can be provided through environment variables or config files.

### Environment Variables

- `{{ package_name | upper }}_DEBUG`: Enable debug mode
- `{{ package_name | upper }}_LOG_LEVEL`: Set logging level (DEBUG, INFO, WARNING, ERROR)
