## Quickstart

Get started with **{{ package_name }}** in seconds:

```python
# Your first example with {{ package_name }}
from {{ package_name }} import YourClass

# Initialize and use
instance = YourClass()
instance.run()
```

This package requires Python {{ python_version }}.
